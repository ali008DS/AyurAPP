1- never generate a summary md file.
2- after each task completion just give short one liner summary.
3- before executing a command always mention, what will that command do and severity in brackets example (safe).
4- in response or comments, or logs or front end icons never use emojis. never
5- in full forming a big string we will use builder function.
6- always use the frameworks cli tool to generate its files, and not custom commands of manual file writing using write or replace tool.
7- write minimal code, maintainable code
8- add comment only where very very necessary, and not in many places, only where ambiguity.
9- if using tailwind for styling (frontend framework) then use tailwind built-in option (text-black dark:text-white)
10- when doing backend make sure that use structure where each type of file in responsible for each type of the work, (SOLID)
11- make sure if one thing can be defined in once and used again and again, always do it, (always define then continue)
12- Use centralized error handling and never throw raw errors to clients.
13- Map internal errors to stable, documented error responses.
14- Offload long-running tasks to background workers and never block request threads with heavy computation.
15- Never break existing consumers without versioning or migration path.
16- proceed without creating todos, and its not needed and preferably to not create the todos, proceed directly.